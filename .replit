run = "python -m src.main --port 8080"
entrypoint = "src/main.py"
modules = ["python-3.12"]

[env]
PYTHONUNBUFFERED = "1"

[nix]
channel = "stable-25_05"

[deployment]
run = ["python", "-m", "src.main", "--port", "8080"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 8080
externalPort = 80
